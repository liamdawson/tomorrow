language: csharp
solution: Tomorrow.sln
dist: trusty
script:
  - dotnet restore
  - for proj in `ls *.UnitTest/*.csproj`; do dotnet build "$proj"; done;
  - for proj in `ls *.UnitTest/*.csproj`; do dotnet test "$proj"; done;

matrix:
  include:
    - dotnet: 1.0.5
      mono: none
      env: DOTNETCORE=1
    - dotnet: 1.1.2
      mono: none
      env: DOTNETCORE=1

